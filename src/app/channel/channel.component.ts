import { Component, OnInit, Input } from '@angular/core';  import { Socket } from "../socket";  @Component({   selector: 'channel',   templateUrl: './channel.component.html',   styleUrls: ['./channel.component.scss'] }) export class ChannelComponent {   constructor(private socket: Socket) {}   private state: boolean = false;    @Input()   private identity: number;      public label: string = 'wesolych swiat';    ngOnInit() {     this.socket.action('ChangeState', ({ controllerResponse, value }) => {       if (value.identity === this.identity) {         if (controllerResponse ? controllerResponse === 'OK' : true) {           this.state = value.state;
        }
      }     });   }    toggleColor() {     const { identity, state } = this;      this.socket.send({       controller: 'ChangeState',       value: {         state: !state,         identity       }     });   } } 